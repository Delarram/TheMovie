import 'package:flutter/material.dart';

class TitleTextWithSeeMoreView extends StatelessWidget {
  const TitleTextWithSeeMoreView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
